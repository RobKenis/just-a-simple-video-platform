pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "${quarkusPluginId}" version "${quarkusPluginVersion}"
    }
}

rootProject.name = 'just-a-simple-video-platform'
include('video-platform-core', 'video-platform-rooms')
include('video-platform-rooms-lambda')
include('video-platform-infrastructure')
